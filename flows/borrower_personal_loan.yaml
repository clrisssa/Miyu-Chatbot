name: borrower_personal_loan
states:
    stop_schedule_state:
        component: meya.schedule_stop
        properties:
            tag: schedule
    check_user:
        component: meya.conditional_exists
        properties:
            scope: user
            key: email
        transitions:
            exists: is_user
            notexists: is_non_user
        delay:
            relative: 2
    is_user:
        component: meya.text
        properties:
            text: Unfortunately we do not do personal loans as we are only licensed to provide loans to registered businesses.
        delay:
            relative: 2
    is_user2:
        component: meya.text
        properties:
            text: We wish you all the best nonetheless! 😃
        delay:
            relative: 3
        transitions:
            next: anything_else_schedule
    is_non_user:
        component: meya.text
        properties:
            text: Unfortunately we do not do personal loans as we are only licensed to provide loans to registered businesses.
        delay:
            relative: 2
    is_non_user2:
        component: meya.text
        properties:
            text: We wish you all the best nonetheless! 😃
        delay:
            relative: 3
        transitions:
            next: anything_else_schedule
    anything_else_schedule:
        flow: anything_else_schedule