name: faq_referral
states:
    stop_schedule_state:
        component: meya.schedule_stop
        properties:
            tag: schedule
    api_ai_state:
        component: meya.api_ai
        properties:
            text: Are you asking as an investor or borrower?
            client_access_token: db5893f5957b471f8199307419abca95
            language: en
            error_message: I don't understand
            min_confidence: 0.8
            require_match: false
        transitions:
            investor: investor
            borrower: borrower
            catch_all: ambiguous
    investor:
        component: meya.text
        properties:
            text: We used to have an investor referral incentive before we got our CMS license. But ever since we've been licensed, we are unable to offer the referral incentive anymore 😢
        transitions:
            next: anything_else_schedule
    borrower:
        component: meya.text
        properties:
            text: There is a $500 reward for every successful referral made.
        transitions:
            next: anything_else_schedule
    ambiguous:
        component: meya.text
        properties:
            text: Sorry, I didn't understand that. Let me get a human to help you!
        transitions:
            next: anything_else_schedule
    anything_else_schedule:
        flow: anything_else_schedule
